# Test

## Testing locally

```bash
docker compose up
# localhost:8080/ -> spark
# localhost:9002/ -> redpanda
# localhost:9000/ -> minio (minio-minio123)
# localhost:3000/ -> grafana (admin-admin)
# localhost:9092/ -> kafka
# localhost:9090/ -> prometheus
# localhost:6379/ -> redis
```
